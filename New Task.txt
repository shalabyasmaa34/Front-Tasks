Question 1 :
 
Class Circle {
constructor (radius,color);

this.radius =radius ;
this.color=color;

setRadius(){
this.radius=radius;
}
getRadius(){
return this.radius;
}
setColor(){
this.color=color;
}
getRadius(){
return thiscolor;
}
getArea(){
return Math.Power *this.radius,2 *Math.Pi;
Let C=new Circle 1;
console.log(c.Radius);
console.log(c.Area);

Class Cylinder extends Circle {
super (radius,color,height);
this.radius =radius ;
this.color=color;
this.height=height;
}
set Height(){
thisheight=height;
getHeight(){
return this.height;
}
get Volume(){
return this.volume;
}
c1=new cylinder(1);
console.log(c.Volume);

